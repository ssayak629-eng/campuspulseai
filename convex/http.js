import { httpRouter } from "convex/server";

const http = httpRouter();

// Add any HTTP routes here if needed
// e.g. http.route({ path: "/clerk-webhook", method: "POST", handler: ... });

export default http;
